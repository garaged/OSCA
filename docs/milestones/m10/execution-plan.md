# M10 Execution Plan

## M10.1 LLM lifecycle contracts and gateway gates

- Add `osca.llm` immutable contracts.
- Add deterministic gateway service helpers.
- Add focused contract/service tests.
- Add M10 docs, OpenSpec change, requirement allocation, traceability, and manual testing update.

## Later M10 slices

- Add durable LLM metadata persistence if needed.
- Add provider adapter declaration persistence.
- Add CLI/API administration only after contracts are stable.
- Keep provider execution deferred until a governed slice explicitly accepts it.
